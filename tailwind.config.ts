// Tailwind CSS v4 uses the CSS-first configuration in app/globals.css.
// This file is intentionally kept as a no-op for tooling that still checks for
// the legacy config path (for example, components.json integrations).
const config = {};

export default config;
