{
  "name": "anilist-stat-cards",
  "version": "1.0.0",
  "description": "This is a Flask application that generates SVG stat cards for AniList users. It fetches user data from AniList and generates an SVG image that displays the user's statistics in a visually appealing way.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@sourcegraph/stylelint-config": "^1.4.0",
    "eslint": "^8.56.0",
    "stylelint": "^16.2.1",
    "stylelint-config-standard": "^36.0.0",
    "stylelint-config-standard-scss": "^13.0.0"
  },
  "dependencies": {
    "jscpd": "^3.5.10"
  }
}
